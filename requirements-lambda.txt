# Minimal runtime dependencies for the AWS Lambda container image.
# (The root requirements.txt is a full pip freeze from the old Hetzner box and
#  includes OS-level packages that must NOT be installed in Lambda.)
fastapi==0.115.0
mangum==0.21.0
pydantic==2.9.2
email-validator==2.3.0
anthropic==0.107.1
boto3==1.35.24
bcrypt==5.0.0
